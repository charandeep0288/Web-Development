const nodemailer = require('nodemailer');

// http -> 443, http -> 587
// user -> name email password



var Osubject, Otext, Ohtml;

subject = `Thank you for signing ${userObj.name}`;
text = '';
html = `<h1>Welcome to foodApp.com<h1>`;

let info = awiat transporter.sendMail({
    from: 
});